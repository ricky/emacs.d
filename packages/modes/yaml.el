;;; yaml-mode.el -- Install yaml-mode package

;;; Commentary:
;; Install & configure yaml-mode

;;; Code:
(use-package yaml-mode
  :ensure t
  :mode "\\.yaml\\'")


(provide 'packages/modes/yaml)
;;; yaml.el ends here
